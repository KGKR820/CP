#include <bits/stdc++.h>
#include <unordered_map>
using namespace std;
int countMajoritySubarrays(vector<int> &nums, int target) {
  unordered_map<int, int> map;
  for (int ele : nums) {
    map[ele]++;
  }
 return 0;
}
